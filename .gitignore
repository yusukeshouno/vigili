node_modules/
dist/
.next/
coverage/
*.log
*.tsbuildinfo
.DS_Store
.env
.env.local
.env.*.local

# Sentinel runtime artifacts (should never live in repo)
/.sentinel/

# Secrets — must never be committed (defense in depth on top of .env rules)
*.p8
*.pem
*.key
*.p12
*.mobileprovision
AuthKey_*.p8
id_rsa
id_ed25519

# Local databases / runtime state
*.db
*.db-shm
*.db-wal
*.sqlite

# pnpm
.pnpm-store/
.vercel

# 配布物 (dist/) は git 管理しない — GitHub Releases で配布
dist/

# グリッチ (confabulation) 調査のローカル成果物 — リポジトリには含めない
GLITCH_REPORT.md
GLITCH_REPORT.ja.md
scripts/confab-detect.mjs
scripts/confab-experiment.sh
scripts/confab-experiment-v3.sh
